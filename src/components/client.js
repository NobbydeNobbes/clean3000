import React from 'react';

const Client = ({name, id}) => {
    return (
        
                <option key={id}>{name}</option>
                
          
        
      
    )
  }
  
  export default Client